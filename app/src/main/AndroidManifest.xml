<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prm_assignment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRM_Assignment">

        <!-- MainActivity is the launcher - shows login or home -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PRM_Assignment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HomeActivity for XML-based home UI -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.PRM_Assignment" />

        <!-- AppointmentDetailActivity for viewing appointment details with service records -->
        <activity
            android:name=".ui.activities.AppointmentDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.PRM_Assignment"
            android:parentActivityName=".HomeActivity" />

    </application>

</manifest>